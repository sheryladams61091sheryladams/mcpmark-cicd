// Fixed linting errors
function exampleFunction() {
  const message = 'single quotes are used here';
  console.log(message); // Console usage is a warning (allowed)
  return 42;
}

exampleFunction();